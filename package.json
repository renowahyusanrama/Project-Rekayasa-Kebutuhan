{
  "name": "sistem-pelacakan-alumni-main",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix react-dashboard install",
    "dev": "npm --prefix react-dashboard run dev",
    "build": "npm --prefix react-dashboard run build",
    "start": "npm --prefix react-dashboard run preview -- --host 0.0.0.0 --port $PORT",
    "preview": "npm --prefix react-dashboard run preview",
    "client:dev": "npm --prefix react-dashboard run dev",
    "client:build": "npm --prefix react-dashboard run build",
    "client:preview": "npm --prefix react-dashboard run preview",
    "server": "node server.js",
    "pipeline:normalize": "node scripts/normalize.js",
    "pipeline:dedupe": "node scripts/dedupe.js",
    "pipeline:metrics": "node scripts/compute_metrics.js",
    "pipeline:sample": "node scripts/generate_sample.js",
    "pipeline:enrich-stub": "node scripts/enrich_stub.js",
    "pipeline:convert": "node scripts/convert_xlsx_to_json.js",
    "pipeline:enrich": "node scripts/auto_enrich.js",
    "pipeline:enrich-stats": "node scripts/auto_enrich.js --stats",
    "pipeline:merge": "node scripts/merge_enriched.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.1"
  },
  "devDependencies": {
    "xlsx": "^0.18.5"
  }
}
